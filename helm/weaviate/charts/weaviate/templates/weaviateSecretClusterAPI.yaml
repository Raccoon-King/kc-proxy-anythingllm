{{- if .Values.clusterApiSecret.create -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "weaviate.fullname" . }}-cluster-api-basic-auth
type: kubernetes.io/basic-auth
data:
{{- ( include "cluster_api.secret" . ) | indent 2 -}}
{{- end -}}
